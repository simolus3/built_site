name: code_snippets
description: Experimental builder for dealing with code snippets.
version: 0.0.14
publish_to: none

environment:
  sdk: '^3.3.0'

dependencies:
  analyzer: ^6.5.0
  build: ^2.2.1
  collection: ^1.15.0
  glob: ^2.1.0
  meta: ^1.7.0
  path: ^1.8.2
  source_span: ^1.8.2

dev_dependencies:
  build_test: ^2.1.5
  lints: ^5.0.0
  logging: ^1.0.2
  test: ^1.12.1
