name: docsy
description: Port of docsy to built_site
version: 0.2.3

environment:
  sdk: '>=2.15.0 <4.0.0'

dependencies:
  bootstrap:
    hosted: https://simonbinder.eu
    version: ^5.2.3
  built_site:
    hosted:
      url: https://simonbinder.eu
      name: built_site
    version: ^0.2.16
  build_config: ^1.0.0

dependency_overrides:
  built_site:
    path: ../../built_site
